#!/usr/bin/env bash
# This script lists files in the current directory and prints only the part after the first dash using a for loop

for file in *; do
  if [[ $file == -* || $file == .* ]]; then
    continue
  fi
  echo "${file#*-}"
done
